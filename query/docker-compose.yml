version: "3.8"

services:
  query:
    build:
      context: .
    ports:
      - "4002:4002"
    container_name: query
    restart: always
    image: tp-blog/query:latest
    networks:
      - blog

networks:
  blog:

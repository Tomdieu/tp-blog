version: "3.8"

services:
  client:
    build:
      context: .
    ports:
      - "3000:3000"
    container_name: client
    restart: always
    image: tp-blog/client:latest
    networks:
      - blog

networks:
  blog:

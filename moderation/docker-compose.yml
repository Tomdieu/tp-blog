version: "3.8"

services:
  moderation:
    build:
      context: .
    ports:
      - "4003:4003"
    container_name: moderation
    restart: always
    image: tp-blog/moderation:latest
    networks:
      - blog

networks:
  blog:
    